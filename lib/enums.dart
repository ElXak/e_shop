enum TextFieldType {
  email,
  password,
  confirmPassword,
  firstName,
  lastName,
  phoneNumber,
  address,
  address2,
}

enum FormName {
  signUp,
  signIn,
  forgotPassword,
  completeProfile,
  otp,
}

enum MenuState {
  home,
  favorites,
  messages,
  profile,
}
